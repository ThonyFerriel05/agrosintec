{
  "name": "agrosintec-local",
  "version": "1.0.0",
  "private": true,
  "description": "Lanzador local: arranca backend (agrosintec) y frontend a la vez.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,green \"npm --prefix agrosintec run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "install:all": "npm --prefix agrosintec install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
